import { Poppins, Alatsi, IBM_Plex_Mono  } from 'next/font/google';

export const poppins = Poppins({ subsets: ['latin'], weight: "500" });
export const alatsi  = Alatsi({ subsets: ['latin'], weight: "400" });
export const ibmPlexMono = IBM_Plex_Mono({ subsets: ['latin'], weight: "400" });

